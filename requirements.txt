# Для звука библиотек нет и не будет: всю обработку делает FFmpeg, Python только
# строит команды.
#
# Для картинки они нужны. Видеофон процедурный — каждый из 1800 кадров считается
# арифметикой по массиву 1920x1080, и на чистом Python один рендер шёл бы часами
# вместо минут. Кодирование по-прежнему целиком на FFmpeg.
numpy>=2.0
scipy>=1.13
Pillow>=10.0

# Тесты.
pytest>=8.0
